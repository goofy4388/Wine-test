:root{
  --bg1:#0b1220;
  --bg2:#131a2b;
  --card: rgba(255,255,255,.08);
  --card2: rgba(255,255,255,.10);
  --stroke: rgba(255,255,255,.12);
  --text: rgba(255,255,255,.92);
  --muted: rgba(255,255,255,.70);
  --danger:#ff6b6b;
  --good:#42f59e;
  --gold:#ffd66e;
  --shadow: 0 20px 60px rgba(0,0,0,.35);
  --radius: 18px;
  --focus: 0 0 0 3px rgba(255,214,110,.25);
  --font: system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family:var(--font);
  color:var(--text);
  background: radial-gradient(1200px 700px at 15% 15%, #2b3c7a 0%, transparent 60%),
              radial-gradient(900px 600px at 85% 30%, #7b2b5c 0%, transparent 55%),
              linear-gradient(180deg, var(--bg1), var(--bg2));
  overflow-x:hidden;
}

.bg{
  position:fixed;
  inset:-40px;
  pointer-events:none;
  background:
    radial-gradient(2px 2px at 10% 20%, rgba(255,255,255,.55), transparent 60%),
    radial-gradient(2px 2px at 35% 70%, rgba(255,255,255,.40), transparent 60%),
    radial-gradient(2px 2px at 60% 25%, rgba(255,255,255,.35), transparent 60%),
    radial-gradient(2px 2px at 80% 80%, rgba(255,255,255,.45), transparent 60%);
  filter: blur(.2px);
  opacity:.7;
}

.wrap{
  max-width: 920px;
  margin: 32px auto;
  padding: 0 16px 32px;
}

.hero{
  margin-bottom: 14px;
}

.brand{
  display:flex;
  gap:14px;
  align-items:center;
  padding: 14px 16px;
  border-radius: var(--radius);
  background: rgba(255,255,255,.06);
  border: 1px solid var(--stroke);
  box-shadow: var(--shadow);
}

.logo{
  width:44px;height:44px;
  display:grid;place-items:center;
  border-radius: 14px;
  background: rgba(255,255,255,.10);
  border:1px solid var(--stroke);
  font-size: 22px;
}

h1{margin:0;font-size:24px;letter-spacing:.2px}
.sub{margin:4px 0 0;color:var(--muted)}

.card{
  background: var(--card);
  border: 1px solid var(--stroke);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 18px;
  margin-top: 14px;
  backdrop-filter: blur(10px);
}

.hidden{display:none !important;}

.muted{color:var(--muted)}
.error{margin-top:10px;color:var(--danger);min-height:18px}

.formRow{
  display:flex;
  flex-direction:column;
  gap:8px;
  margin-top: 14px;
}

label{color:var(--muted);font-size:14px}

input{
  width:100%;
  padding: 12px 12px;
  border-radius: 14px;
  border: 1px solid var(--stroke);
  background: rgba(0,0,0,.18);
  color: var(--text);
  outline:none;
}

input:focus{box-shadow:var(--focus);border-color:rgba(255,214,110,.5)}

.btnRow{
  display:flex;
  gap:10px;
  margin-top: 14px;
  flex-wrap:wrap;
}

.btn{
  border: 1px solid var(--stroke);
  background: rgba(255,255,255,.08);
  color: var(--text);
  padding: 11px 14px;
  border-radius: 14px;
  cursor:pointer;
  font-weight:600;
}

.btn:hover{background: rgba(255,255,255,.12)}
.btn:active{transform: translateY(1px)}
.btn:focus{outline:none;box-shadow:var(--focus)}

.btn.primary{
  background: linear-gradient(180deg, rgba(255,214,110,.24), rgba(255,214,110,.12));
  border-color: rgba(255,214,110,.35);
}

.btn.small{
  padding: 8px 10px;
  border-radius: 12px;
  font-size: 13px;
}

.quizTop{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:12px;
  flex-wrap:wrap;
}

.pill{
  display:flex;
  gap:8px;
  align-items:center;
  padding: 8px 10px;
  border-radius: 999px;
  border:1px solid var(--stroke);
  background: rgba(0,0,0,.18);
}
.pillLabel{color:var(--muted);font-size:13px}

.questionBox{
  margin-top: 14px;
  padding: 14px;
  border-radius: 16px;
  border: 1px solid var(--stroke);
  background: rgba(0,0,0,.18);
}

#questionText{margin:0;font-size:18px;line-height:1.35}

.answers{
  display:grid;
  gap:10px;
  margin-top: 14px;
}

.choice{
  display:flex;
  gap:10px;
  align-items:flex-start;
  padding: 12px 12px;
  border-radius: 16px;
  border: 1px solid var(--stroke);
  background: rgba(255,255,255,.06);
  cursor:pointer;
}

.choice:hover{background: rgba(255,255,255,.10)}
.choice input{margin-top:3px}
.choiceText{line-height:1.25}

.navRow{
  display:flex;
  justify-content:space-between;
  gap:10px;
  margin-top: 14px;
}

.resultsHeader{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:12px;
  flex-wrap:wrap;
}

.stamp{
  padding: 10px 14px;
  border-radius: 14px;
  font-weight:900;
  letter-spacing:.8px;
  border: 2px solid rgba(66,245,158,.35);
  background: rgba(66,245,158,.12);
  color: var(--good);
}

.stamp.fail{
  border-color: rgba(255,107,107,.40);
  background: rgba(255,107,107,.12);
  color: var(--danger);
}

.resultsGrid{
  display:grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 10px;
  margin-top: 14px;
}

.resultTile{
  border-radius: 16px;
  border: 1px solid var(--stroke);
  background: rgba(0,0,0,.18);
  padding: 12px;
}

.tileLabel{
  color: var(--muted);
  font-size: 13px;
}

.tileValue{
  margin-top: 6px;
  font-size: 18px;
  font-weight: 800;
}

.review{
  margin-top: 14px;
  border-radius: 16px;
  border: 1px solid var(--stroke);
  background: rgba(0,0,0,.18);
  padding: 12px;
}

.reviewTop{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:10px;
}

.reviewList{
  margin-top: 10px;
  display:grid;
  gap:10px;
}

.reviewItem{
  border-radius: 14px;
  border: 1px solid var(--stroke);
  background: rgba(255,255,255,.06);
  padding: 10px;
}

.reviewItem b{color: var(--gold)}
.reviewMeta{color:var(--muted);font-size:13px;margin-top:6px}

.foot{
  margin-top: 14px;
  text-align:center;
  padding: 10px;
}
@media (max-width: 560px){
  .resultsGrid{grid-template-columns: 1fr;}
  .navRow{flex-direction:column}
}
